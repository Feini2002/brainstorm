# 仓库实施协议


这个仓库首先是Feini Brain MVP规格包。目标是在保留文档和参考契约的前提下，实现本地单用户AI知识碎片工具。不要把规格存在当代码已存在，不要重新发明产品范围。

## 必读入口与优先级

先读00_ROUTER.md，再读docs/03_contracts/00_contract_index.md与当前任务。规范优先级：总纲的范围约束→权威契约及机器限制→当前任务的具体规则→参考示例。编号版本精化规则以契约为准；发现真实矛盾先记录并做最小统一修正，不在不同页面各实现一种解释。

一次只推进一个任务或紧密耦合的两项。使用reference/contracts/tasks.json检查前置；implementation/progress/tasks.initial.json是起始状态模板，复制为tasks.current.json再更新。未经实际测试不设verified。所有新运行证据进入implementation/progress/evidence。

## 执行流程

1. 读取当前任务、其六个用例、相关契约和必要上游公开接口；不要一次加载所有长文档。
2. 列出本任务允许修改文件、输入输出和必须保持的不变量。
3. 先实现真实最小路径，再补错误和边界；不创建假页面或固定数据冒充完成。
4. 执行与任务有关的测试和上游冒烟回归，记录实际结果。
5. 更新进度与NEXT_TASK，提交本阶段可运行代码。受环境限制时写blocked和具体原因。

## 禁止行为

不要引入云数据库、登录、向量库、Agent框架、独立Python后端、Docker部署、微服务、CRDT或自由白板。不要把API Key放前端持久存储、NEXT_PUBLIC变量、日志或导出。不要把LLM请求散落在组件。不要在SQL事务里等待网络。不要用eval处理模型输出。不要在响应返回后用裸Promise承诺后台完成。

不要删除或覆盖本规格包以满足脚手架的空目录要求。用临时目录生成应用并受控复制，详见安装文档。不要把reference示例当完整生产代码；其中schema需要业务验证器补充，SQL需要实际Node运行测试。

## 完成含义

文件存在不等于功能完成，TypeScript通过不等于浏览器可用，mock通过不等于真实模型通过。每个Gate分别报告已执行、失败、阻塞、未执行。没有真实证据时保持未验收状态，不虚构测试日志、截图或依赖兼容性结论。
