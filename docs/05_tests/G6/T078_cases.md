# T078 验收用例｜六页浏览器端到端验收


本文件是**测试规格**，不是测试结果。实施入口：[T078 六页浏览器端到端验收](../../04_tasks/G6/T078_e2e_suite.md)。所属 Gate：G6。规范字段和限制见 [契约索引](../../03_contracts/00_contract_index.md)。

## 公共测试装置

用例使用隔离数据与不可用的测试秘密。任何真实连接验证都应单独记录，并由用户在自己的设置页配置。自动化测试不得连接默认用户数据库。外部依赖替身不得绕过业务校验和事务。

观察方法：保存命令、退出码、测试ID、最小输入和预期实际差异，必要时附安全截图及数据库观察。

基线与状态：用例结果、Gate状态、临时数据、报告与明确未执行项。

整个用例组不可违反：测试不触碰用户数据库和可用Key，不自动修改架构来迎合断言。

清理方法：关闭测试服务和浏览器、清理独立目录，保留脱敏失败样本与可重复执行命令。每个场景必须从明确基线开始，不依赖上一场景残留的缓存或运行记录。幂等测试要明确有意复用的键，非幂等测试不得误复用。

实际执行结果应追加到 implementation/progress/evidence/ 下，不直接把规格的预期句改成已完成结论。浏览器截图、数据库断言、外部请求次数分别证明不同事情，不能相互替代。

## T078-C01｜完整旅程

**前置：** 从空库和无配置开始。

**动作：** 完成主要用户操作链。

**必须断言：** 每个页面用同一真实数据衔接。

**必须排除：** 分散demo不能证明系统完整。



## T078-C02｜刷新检验

**前置：** 每次保存后刷新页面。

**动作：** 继续使用。

**必须断言：** 状态从服务器恢复而非只在React内存。

**必须排除：** 端到端应覆盖持久化。



## T078-C03｜按钮真实

**前置：** 点击生成导出审核等动作。

**动作：** 核对网络和结果。

**必须断言：** 实际执行契约，不是弹一个成功toast。

**必须排除：** 视觉反馈不等于功能完成。



## T078-C04｜控制台异常

**前置：** 渲染过程中出现未捕获错误。

**动作：** 测试报告。

**必须断言：** 明确失败并定位组件。

**必须排除：** 忽略console会留下隐藏不稳定。



## T078-C05｜窄屏输入

**前置：** 缩小视口并使用键盘。

**动作：** 完成保存和详情。

**必须断言：** 核心功能可用。

**必须排除：** 桌面默认布局不一定适用所有窗口。



## T078-C06｜隐私trace

**前置：** 测试设置Key。

**动作：** 检查录制产物。

**必须断言：** 不包含可用秘密。

**必须排除：** 自动化证据也需要脱敏。
