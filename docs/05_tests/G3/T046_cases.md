# T046 验收用例｜Dagre布局、尺寸与坐标转换


本文件是**测试规格**，不是测试结果。实施入口：[T046 Dagre布局、尺寸与坐标转换](../../04_tasks/G3/T046_dagre_layout.md)。所属 Gate：G3。规范字段和限制见 [契约索引](../../03_contracts/00_contract_index.md)。

## 公共测试装置

用例使用隔离数据与不可用的测试秘密。任何真实连接验证都应单独记录，并由用户在自己的设置页配置。自动化测试不得连接默认用户数据库。外部依赖替身不得绕过业务校验和事务。

观察方法：记录节点边ID、数量、坐标、viewport与选择状态，检查对应Item/Relation/View版本是否按职责变化。

基线与状态：GraphData、布局位置、筛选条件、关系状态与来源版本。

整个用例组不可违反：只有明确领域动作可以写知识；图形生命周期没有重复监听或持续外部网络。

清理方法：卸载画布并断言实例清理，重开已存View核对位置；不以重置所有知识修复图形问题。每个场景必须从明确基线开始，不依赖上一场景残留的缓存或运行记录。幂等测试要明确有意复用的键，非幂等测试不得误复用。

实际执行结果应追加到 implementation/progress/evidence/ 下，不直接把规格的预期句改成已完成结论。浏览器截图、数据库断言、外部请求次数分别证明不同事情，不能相互替代。

## T046-C01｜中心转换

**前置：** 两节点固定宽高。

**动作：** 执行自动布局。

**必须断言：** 卡片不因坐标基准错误偏移半个宽度。

**必须排除：** 库之间坐标约定不同。



## T046-C02｜手工保留

**前置：** 用户拖动几个节点后更新标题。

**动作：** 重新渲染图。

**必须断言：** 位置不自动复位。

**必须排除：** 每次渲染重排会破坏使用体验。



## T046-C03｜有环知识

**前置：** A依赖B且存在回路。

**动作：** 自动布局。

**必须断言：** 可生成有界位置且不删除边。

**必须排除：** 真实知识网不必是树。



## T046-C04｜孤立分量

**前置：** 三组互不相连知识。

**动作：** 布局。

**必须断言：** 各组均可见且fitView覆盖。

**必须排除：** 算法不能只处理最大连通分量。



## T046-C05｜无效坐标

**前置：** 测试布局适配器返回NaN。

**动作：** 应用结果。

**必须断言：** 拒绝并保留旧位置。

**必须排除：** 非法坐标可能让整张图消失。



## T046-C06｜重复布局

**前置：** 同输入连续点击布局。

**动作：** 比较业务数据。

**必须断言：** 仅positions变化，Item与Relation不被修改。

**必须排除：** 排版不是语义编辑。
