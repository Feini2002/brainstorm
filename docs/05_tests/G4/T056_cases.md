# T056 验收用例｜树校验与安全Markdown编译


本文件是**测试规格**，不是测试结果。实施入口：[T056 树校验与安全Markdown编译](../../04_tasks/G4/T056_mindmap_compiler.md)。所属 Gate：G4。规范字段和限制见 [契约索引](../../03_contracts/00_contract_index.md)。

## 公共测试装置

用例使用隔离数据与不可用的测试秘密。任何真实连接验证都应单独记录，并由用户在自己的设置页配置。自动化测试不得连接默认用户数据库。外部依赖替身不得绕过业务校验和事务。

观察方法：比对canonical JSON、来源ID与版本、编译哈希、DOM实例和外部请求；检查旧View是否被保留。

基线与状态：View内容、sourceSnapshot、编译版本、渲染token与过期信息。

整个用例组不可违反：生成失败不破坏旧图，浏览或重试本地渲染不重新计费，来源不被复制成独立知识。

清理方法：撤销ObjectURL并清理Renderer实例，保留安全canonical样本用于复现；删除测试View不删知识。每个场景必须从明确基线开始，不依赖上一场景残留的缓存或运行记录。幂等测试要明确有意复用的键，非幂等测试不得误复用。

实际执行结果应追加到 implementation/progress/evidence/ 下，不直接把规格的预期句改成已完成结论。浏览器截图、数据库断言、外部请求次数分别证明不同事情，不能相互替代。

## T056-C01｜树中有环

**前置：** m2父为m3且m3父为m2。

**动作：** 校验结构。

**必须断言：** 拒绝环并定位节点。

**必须排除：** 递归渲染可能无限循环。



## T056-C02｜多个根

**前置：** 两个节点parentId均null。

**动作：** 校验。

**必须断言：** 明确拒绝。

**必须排除：** 多根结构需模型重新组织而非随意丢一棵。



## T056-C03｜孤立节点

**前置：** 父ID不存在。

**动作：** 校验。

**必须断言：** 拒绝悬空子树。

**必须排除：** 隐藏丢失节点会让用户误以为已全部整理。



## T056-C04｜语法注入

**前置：** 标签包含图片Markdown和HTML。

**动作：** 编译。

**必须断言：** 以字面文字呈现，不加载外部资源。

**必须排除：** LLM标签也是不可信输入。



## T056-C05｜深度边界

**前置：** 树深度为5与6。

**动作：** 分别校验。

**必须断言：** 5通过6拒绝。

**必须排除：** 层级限制应有明确起点。



## T056-C06｜可复现

**前置：** 同一JSON和编译版本。

**动作：** 编译两次。

**必须断言：** Markdown与哈希一致。

**必须排除：** 确定性编译便于定位变化来源。
