# T019｜人工编辑、版本冲突与字段保护


任务编号：**T019**。所属阶段：**G1｜离线知识库闭环**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

确保用户可以修正AI结果并保留控制权，旧模型响应和另一个标签页都不能悄悄覆盖已经保存的编辑。

该任务负责的是“领域不变量、版本与用户控制权”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G1/T019_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T018 详情抽屉、原文和来源回溯](T018_item_details.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** Item ID、expectedRevision、允许修改字段、可选resetAiFields。

**输出：** 新的Item revision、必要rawVersion增量、manualFields集合、409冲突反馈。

本任务允许：维护实体身份、人工决定、状态转移和原子更新。边界以外：不得让Renderer格式或模型自由文本决定领域存储结构。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/server/services/editItem.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `src/features/library/EditItemForm.tsx` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `src/domain/manualFields.ts` | 共享纯契约或算法；禁止环境副作用，保持可单元测试。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T019-R01

PATCH只允许rawText、title、summary、type、tags、keywords、importance及受控来源字段，不允许修改capturedText和系统版本。

### T019-R02

rawText实际内容变化才增加rawVersion；任何领域字段实际改变增加revision；无变化提交返回当前值不虚增。

### T019-R03

人工修改整理字段后加入manualFields，用户明确重置才移除；重置不立即发模型请求。

### T019-R04

修改rawText保留旧整理结果但标记stale，相关边和视图通过版本比较判旧。

### T019-R05

用expectedRevision进行乐观并发控制，409保留编辑草稿并展示重新载入或比较动作。

### T019-R06

模型整理结果提交前也遵守revision检查；不能为了AI完成率忽略用户刚做的修改。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：领域Service连接仓储、模型结果和用户动作；所有页面通过同一契约观察状态而不各自推导一套规则。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：Item revision/rawVersion、Relation审核与证据、Run终态或View来源。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：原文不会无提示消失，人工修改不被静默覆盖，迟到响应不复活已终止操作。除了断言目标结果，还要确认不相关实体未改变；跨实体误写也是失败。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：先读取目标及依赖实体的版本、状态和关系，再触发动作并复核所有应变与不应变字段。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：恢复本用例专用fixture并清理运行记录；对不可逆动作使用隔离数据库而不是回滚真实资料。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[精确DTO、版本、状态优先级和哈希](../../03_contracts/03_dto_and_version_rules.md)、[关系词典、审核和因果门槛](../../03_contracts/08_relations_and_evidence.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
