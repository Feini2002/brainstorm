# T013｜极简输入框与中文输入体验


任务编号：**T013**。所属阶段：**G1｜离线知识库闭环**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

让一个词、一句话和多段文字都能立即记录，保持输入动作低摩擦，不把整理字段变成采集必填项。

该任务负责的是“浏览器交互与呈现状态”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G1/T013_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T005 六页应用骨架与导航状态](../G0/T005_app_shell.md)、[T006 统一响应、错误与前端请求器](../G0/T006_api_envelope.md)、[T012 契约一致性与第一阶段验收](../G0/T012_contract_guard.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** 用户文本、可选来源类型与来源引用、当前提交状态、中文输入法composition状态。

**输出：** 只保存与保存并整理两个明确动作；码点长度提示；提交成功前不清空的输入草稿。

本任务允许：管理用户输入、加载空态、焦点、当前选择及安全文本展示。边界以外：不得直接操作SQLite、读取服务端Key或绕过应用API写入领域对象。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/features/inbox/CaptureBox.tsx` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `src/features/inbox/useCapture.ts` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `tests/e2e/capture.spec.ts` | 验收与复现入口；使用隔离数据，不把预期行为写成无断言的占位测试。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T013-R01

原文输入使用textarea，标题、标签、类型不作为必填；来源默认other，用户可以手动选择而不是猜测剪贴板出处。

### T013-R02

空判断使用trim但原样发送原文；换行与内部空格保留；长度以Unicode码点计数，上限一万。

### T013-R03

Ctrl+Enter可保存，中文输入法组合期间不触发；Enter本身仍换行，不能让用户选字时误提交。

### T013-R04

保存中禁用相同动作但允许用户知道当前状态；不要把禁用按钮当成后端幂等的替代。

### T013-R05

只有收到创建成功才清空已提交那一版草稿；用户在等待期间继续输入的新内容不得被清空。

### T013-R06

没有Key时只保存正常工作，保存并整理说明尚未配置；可以先保存再跳设置，不能丢掉文本。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：输入来自DTO和用户事件；输出经统一apiClient进入服务端；与其它页面共享Item与View身份。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：页面草稿、选中ID、按钮状态、服务器返回的版本。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：没有由导航或重渲染触发的额外付费调用，没有把失败提示当成保存成功。不能只检查成功提示出现；必须确认相应数据真正提交，且未提交草稿和其它页面仍保持正确。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：用可访问角色和稳定测试标识定位控件，记录用户动作前后的DOM、请求参数和服务端读取结果。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：关闭抽屉并清理组件，重新读取服务端状态；测试草稿不进入用户浏览器长期存储。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[精确DTO、版本、状态优先级和哈希](../../03_contracts/03_dto_and_version_rules.md)、[HTTP路由与请求响应](../../03_contracts/04_api_contract.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
