# T031｜OpenAI兼容HTTP适配器


任务编号：**T031**。所属阶段：**G2｜LLM整理与可靠运行**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

用一个受控的HTTP适配层接入模型，使业务服务不依赖具体供应商，也不因不同参数支持程度而到处散落兼容分支。

该任务负责的是“模型协议、上下文和结构化生成”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G2/T031_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T028 Base URL规范化与出站信任边界](T028_endpoint_policy.md)、[T029 设置事务、秘密隔离与读取白名单](T029_secret_storage.md)、[T030 模型错误脱敏与安全日志](T030_redaction.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** 连接快照、messages、输出格式档位、操作AbortSignal、响应字节预算。

**输出：** 规范ProviderResponse、用量可空、finishReason与安全错误分类。

本任务允许：构造受限请求、验证响应、记录运行与预算。边界以外：不得执行模型命令、自动联网找资料或直接把输出写入领域表。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/server/llm/adapters/openaiCompatible.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `src/server/llm/types.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `tests/unit/llm-transport.test.ts` | 验收与复现入口；使用隔离数据，不把预期行为写成无断言的占位测试。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T031-R01

使用Node原生fetch，body基础只有model、messages、stream:false；temperature默认不发送，token字段按用户显式档位添加。

### T031-R02

structuredMode=prompt_json不发送response_format；json_object档位显式发送对应字段，失败不自动换模式重试计费。

### T031-R03

Authorization由服务端秘密填入，redirect设error，超时由AbortController与外部绝对期限共同约束。

### T031-R04

响应读取有字节上限，不能先无限response.text再检查长度；非成功状态与非JSON分别处理。

### T031-R05

只从约定choices[0].message.content取可接受文本，null、工具调用、拒绝、空content和截断都不能假装成功。

### T031-R06

不执行tool_calls，不支持模型调用工具，不把reasoning内容当最终JSON；usage缺失时保持null。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：LLM Adapter只管协议，Service管业务，Schema与编译器管输出；没有Agent工具执行链。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：输入快照、候选集合、格式档位、尝试次数与最终Run状态。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：原文已经持久保存，Key不进入messages，失败与重试不会产生不可见额外请求。返回内容看似合理也必须经过schema和来源校验；固定替身通过不能证明外部模型语义可靠。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：使用可注入transport记录endpoint、调用次数、消息字段、超时和响应形态；同时比对Run与数据库结果。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：终止本用例transport与计时器，清理临时Run；真实连接测试另行记录而不混入固定回归。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[模型操作全过程](../../03_contracts/06_llm_pipeline.md)、[无Embedding召回与证据片段](../../03_contracts/07_candidate_retrieval.md)、[关系词典、审核和因果门槛](../../03_contracts/08_relations_and_evidence.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
