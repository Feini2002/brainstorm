# T042｜AI闭环与真实连接验收


任务编号：**T042**。所属阶段：**G2｜LLM整理与可靠运行**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

证明模型能力已经通过真实接口进入产品，同时把确定性工程验收与主观语义质量评估分开。

该任务负责的是“验收证据与回归覆盖”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G2/T042_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T027 模型设置表单与Key输入口](T027_settings_ui.md)、[T028 Base URL规范化与出站信任边界](T028_endpoint_policy.md)、[T029 设置事务、秘密隔离与读取白名单](T029_secret_storage.md)、[T030 模型错误脱敏与安全日志](T030_redaction.md)、[T031 OpenAI兼容HTTP适配器](T031_llm_transport.md)、[T032 连接测试与能力验证](T032_connection_test.md)、[T033 整理提示词与不可信材料隔离](T033_prompts.md)、[T034 中文候选召回与上下文预算](T034_candidate_search.md)、[T035 运行注册、幂等和并发占用](T035_run_registration.md)、[T036 整理主流程与事务切点](T036_organize_orchestration.md)、[T037 结构化解析、限长与一次修复](T037_structured_parse.md)、[T038 整理字段应用与人工保护](T038_metadata_apply.md)、[T039 AI关系证据、去重与拒绝保护](T039_relation_apply.md)、[T040 运行恢复、迟到响应与局部轮询](T040_run_recovery.md)、[T041 模型用量、输入范围与错误诊断](T041_llm_diagnostics.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** 测试替身场景、用户自行配置的真实连接、相关无关矛盾与注入样本。

**输出：** 结构化测试报告、真实连接是否执行的证据、语义错误分类与Gate2结论。

本任务允许：执行确定性断言、复现失败并区分验证层次。边界以外：不得把未执行测试记为通过、拿Mock冒充真实模型或用截图替代持久化证明。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `tests/e2e/gate2.spec.ts` | 验收与复现入口；使用隔离数据，不把预期行为写成无断言的占位测试。 |
| `tests/fixtures/semantic-cases.json` | 验收与复现入口；使用隔离数据，不把预期行为写成无断言的占位测试。 |
| `docs/progress/G2.md` | 记录实际步骤和证据；未执行事项标明，不用文档代替功能实现。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T042-R01

先用替身跑所有可预测错误、幂等、事务和并发分支，再用真实连接测最小流程。

### T042-R02

真实语义样本至少包括单词、带限定词的观点、转述、无关材料、相反主张和提示注入。

### T042-R03

不把产生边数量当成功指标；检查保真、来源有效、无虚构ID、无强制关系和人工保护。

### T042-R04

真实API Key由用户在自己的系统输入，不写测试夹具、不提交仓库、不要求将Key回传给文档编写者。

### T042-R05

没有Key或外网受阻时诚实标记真实连接未执行，不用Mock替代该证据。

### T042-R06

记录模型与配置版本，但避免承诺不同模型都取得相同文字或完全一致图结构。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：测试ID映射到任务与契约；失败先定位责任模块，不重写整个系统。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：用例结果、Gate状态、临时数据、报告与明确未执行项。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：测试不触碰用户数据库和可用Key，不自动修改架构来迎合断言。只要核心不变量失败就阻塞相应Gate，不用总体通过率稀释关键错误。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：保存命令、退出码、测试ID、最小输入和预期实际差异，必要时附安全截图及数据库观察。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：关闭测试服务和浏览器、清理独立目录，保留脱敏失败样本与可重复执行命令。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[精确DTO、版本、状态优先级和哈希](../../03_contracts/03_dto_and_version_rules.md)、[HTTP路由与请求响应](../../03_contracts/04_api_contract.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
