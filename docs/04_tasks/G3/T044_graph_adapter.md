# T044｜领域图到React Flow适配


任务编号：**T044**。所属阶段：**G3｜关系图与布局**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

让React Flow成为可替换的展示层，业务ID、审核状态和来源数据不被库内部对象格式反向污染。

该任务负责的是“关系图读取、交互和布局”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G3/T044_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T043 图谱读取接口与子图范围](T043_graph_query.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** GraphData、可选保存位置、当前主题和选择ID。

**输出：** React Flow nodes与edges、稳定nodeTypes、明确语义标签。

本任务允许：适配领域图、渲染卡片、计算与保存展示坐标。边界以外：不得因拖动、筛选或缩放修改原文、关系类型和审核事实。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/features/graph/graphAdapter.ts` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `src/features/graph/types.ts` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `tests/unit/graph-adapter.test.ts` | 验收与复现入口；使用隔离数据，不把预期行为写成无断言的占位测试。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T044-R01

node.id等于Item.id，edge.id等于Relation.id，不使用数组下标和随机渲染ID。

### T044-R02

node.data只包含展示需要的标题标签和来源ID，不拷贝Key、完整Run或全部原文。

### T044-R03

edge.type与样式从关系类型及审核状态映射，suggested可用虚线并有文字标记，不能只靠颜色。

### T044-R04

有向关系使用与业务语义一致的箭头，对称关系不显示误导性单向含义。

### T044-R05

位置从View.positions读取，没有位置时标记需布局；适配器不在转换时调用模型。

### T044-R06

输入对象不可变，返回新对象，不能让拖动状态通过引用修改领域DTO。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：React Flow负责交互，Dagre负责坐标，Repository负责知识；三者通过GraphAdapter而不是相互直接写入。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：GraphData、布局位置、筛选条件、关系状态与来源版本。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：只有明确领域动作可以写知识；图形生命周期没有重复监听或持续外部网络。截图只能辅助；需要数据ID和版本断言证明图与真实知识同源。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：记录节点边ID、数量、坐标、viewport与选择状态，检查对应Item/Relation/View版本是否按职责变化。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：卸载画布并断言实例清理，重开已存View核对位置；不以重置所有知识修复图形问题。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[图AST、编译与生命周期](../../03_contracts/09_view_schemas_and_compilers.md)、[关系词典、审核和因果门槛](../../03_contracts/08_relations_and_evidence.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
