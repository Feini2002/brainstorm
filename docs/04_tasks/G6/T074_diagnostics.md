# T074｜本地诊断与可观测性


任务编号：**T074**。所属阶段：**G6｜恢复、安全与交付**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

让启动、数据库、模型和Renderer问题能够分层定位，而不是只有一个笼统的系统失败提示。

该任务负责的是“安全诊断与事实记录”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G6/T074_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T030 模型错误脱敏与安全日志](../G2/T030_redaction.md)、[T041 模型用量、输入范围与错误诊断](../G2/T041_llm_diagnostics.md)、[T073 WAL备份、数据搬迁与损坏恢复说明](T073_backup_recovery.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** 运行版本、数据库版本与计数、最近安全错误、模型连接配置摘要、组件加载状态。

**输出：** 受保护诊断DTO、可复制安全摘要、明确失败层级。

本任务允许：记录实际运行信息、分类错误、提供限长白名单摘要。边界以外：不得根据字数编造精确token费用或将完整笔记作为默认日志。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/app/api/diagnostics/route.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `src/server/observability/diagnostics.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `src/features/settings/DiagnosticsPanel.tsx` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T074-R01

诊断接口与其它私人API一样需要本地令牌；不因它叫health就返回全部配置。

### T074-R02

输出Node和应用版本、schemaVersion、库计数、数据目录可写状态，不默认暴露绝对路径。

### T074-R03

记录API耗时、Run耗时与错误码，Renderer错误按kind和版本区分；不接第三方遥测平台。

### T074-R04

日志保留上限与轮转策略明确，不无限增长；临时错误不能每秒重复打印全堆栈。

### T074-R05

复制诊断不包含Key、原文、完整endpoint query和本地会话令牌。

### T074-R06

遇到AI慢、数据库锁、图容器空白分别给排查入口，避免一律建议重装依赖。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：各层报告统一requestId和runId，但不把整条请求内容贯穿日志；用户据错误层级选择排查路径。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：日志字段、计数、错误分类、运行用量与诊断文件。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：诊断读取不改变知识和Run，不引入第三方遥测，也不通过分享摘要扩散私人内容。观测不到的数据应写未知或未执行，不允许填入看似合理的默认数值。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：核对日志事件、响应摘要、用量来源和关联ID，同时扫描所有产物中的秘密与原文标记。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：清理测试日志并检查轮转限制，保持正式日志策略和受保护接口不变。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[Key、本地请求和出站边界](../../03_contracts/05_settings_and_security.md)、[模型操作全过程](../../03_contracts/06_llm_pipeline.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
