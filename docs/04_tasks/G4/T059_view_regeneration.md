# T059｜视图过期、再生成与历史保留


任务编号：**T059**。所属阶段：**G4｜脑图投影**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

让图表拥有明确时间与依据，不在来源变更后自动改写旧图，也不因为重新生成失败丢掉上一次结果。

该任务负责的是“生成视图、来源与安全编译”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G4/T059_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T053 保存视图的公共API与类型契约](T053_view_repository.md)、[T054 来源选择快照、版本和哈希](T054_source_snapshot.md)、[T058 脑图大纲、来源映射与回跳](T058_mindmap_sources.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** View sourceSnapshot、当前来源版本、选择描述、用户重新生成动作。

**输出：** fresh、stale、missingSources等派生说明、新View和旧View并存。

本任务允许：验证受限结构、编译展示格式、保存来源快照与管理Renderer。边界以外：不得从图标签反向创建知识实体或执行任意模型提供的程序语法。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/server/services/views/getFreshness.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `src/features/shared/ViewFreshnessBanner.tsx` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `src/features/mindmap/RegenerateAction.tsx` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T059-R01

读取时比较来源revision和引用关系revision，任一变化展示stale原因；不依赖笼统updatedAt。

### T059-R02

重新生成创建新的View ID，旧View保留；用户可比较、重命名或删除，不自动覆盖。

### T059-R03

重新生成前重新确认当前来源集合，特别是标签选择可能新增或移除材料。

### T059-R04

生成失败只影响新Run，不改变旧View内容、哈希与generatedAt。

### T059-R05

仅修改View名称或布局不改变来源快照；这类操作不需要模型。

### T059-R06

来源全部删除时不提供无材料生成，仍允许读取旧图并导出安全产物。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：来源选择到LLM到Schema到Compiler到Renderer是单向链；每个阶段都有独立输入输出与失败边界。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：View内容、sourceSnapshot、编译版本、渲染token与过期信息。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：生成失败不破坏旧图，浏览或重试本地渲染不重新计费，来源不被复制成独立知识。SVG能显示不代表来源正确；必须验证无悬空引用、无越界内容和无不确定性丢失。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：比对canonical JSON、来源ID与版本、编译哈希、DOM实例和外部请求；检查旧View是否被保留。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：撤销ObjectURL并清理Renderer实例，保留安全canonical样本用于复现；删除测试View不删知识。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[图AST、编译与生命周期](../../03_contracts/09_view_schemas_and_compilers.md)、[模型操作全过程](../../03_contracts/06_llm_pipeline.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
