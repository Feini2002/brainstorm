# T068｜流程源码、JSON与安全SVG导出


任务编号：**T068**。所属阶段：**G5｜流程投影**。状态：**待实现**。这是实施规格，不是已经执行的测试报告。

## 一、目标与用户可见结果

提供可以带走的流程表达，明确哪些格式只是展示产物，所有格式都不能携带秘密或可执行载荷。

该任务负责的是“可迁移数据与恢复事务”。合格实现不仅要在正常样例上工作，还必须守住这里规定的不变量。它不授权扩大MVP范围，也不授权替换整个技术栈。功能完成以后，应能通过本任务验收文件独立判断，而不是依赖实现者口头说明。

## 二、读取路由与前置条件

先读 [路由总纲](../../../00_ROUTER.md)、[契约索引](../../03_contracts/00_contract_index.md)，再读本任务与 [本任务验收用例](../../05_tests/G5/T068_cases.md)。需要上游能力时按以下前置任务定位，不要求一次载入全部方案。

前置：[T060 脑图Markdown与JSON导出](../G4/T060_mindmap_export.md)、[T065 Mermaid严格渲染与SVG净化](T065_mermaid_renderer.md)、[T067 流程视图保存、来源和新鲜度](T067_flow_history.md)

若前置仍未验证，先补最小缺口并把依赖状态记为阻塞。不要在本任务内部临时伪造另一份上游实现来绕开依赖；这样会使之后的真实集成产生两套行为。

## 三、输入与输出契约

**输入：** Flow canonical JSON、编译源码、已净化SVG、来源元数据。

**输出：** Mermaid文本与JSON下载；安全SVG作为通过测试后启用的附加格式。

本任务允许：按白名单导出、验证版本和引用、在空库原子恢复。边界以外：不得导出秘密、静默合并非空库或执行导入文件中的代码与URL。输入来自外部时先验证，内部状态只由该层合法操作修改。字段名、枚举、数量限制和版本行为以规范契约为准，不依据UI文案自行创造另一种类型。

## 四、允许修改的文件与职责

| 文件或目录 | 责任 |
| --- | --- |
| `src/features/flow/ExportFlow.tsx` | 用户交互或装配层；不直接写数据库，复用统一数据身份。 |
| `src/server/services/exportView.ts` | 服务端能力；保护输入、秘密与事务边界，返回纯数据。 |
| `tests/e2e/flow-export.spec.ts` | 验收与复现入口；使用隔离数据，不把预期行为写成无断言的占位测试。 |

如果需要修改清单外的共享模块，先指出共享缺陷及受影响任务，再做最小扩展并补回归。禁止顺手重构不相关页面、批量升级依赖或删除已有验收。文件尚不存在时按目录职责创建；它们不是可以空着提交的占位文件。

## 五、必须逐条落地的规则

### T068-R01

必做JSON与Mermaid源码导出，SVG仅在净化与浏览器测试通过后启用，不显示未实现按钮。

### T068-R02

源码由受限编译器产生，导出头部说明生成时间、来源和假设性质，不重新请求模型。

### T068-R03

JSON包含schemaVersion、kind、sourceSnapshot、content，不包含秘密配置与完整messages。

### T068-R04

SVG必须复用已净化结果，删除事件、外部URL与脚本，不直接下载原始Mermaid返回值。

### T068-R05

文件名清理，浏览器ObjectURL及时撤销，服务器不接受任意下载写入路径。

### T068-R06

导出不等于可逆导入完整知识；恢复整库使用单独的知识导出契约。

## 六、装配顺序与局部实现策略

第一步确认前置输出已经可读取，列出本任务实际要消费的字段与函数。不要先画完整界面再发现上游没有需要的版本或ID。第二步在责任层实现核心转换或操作，先用最小输入验证成功分支，再加入边界输入和错误分支。

第三步连接上下游：导出与导入通过同一个Bundle契约连接，单视图导出和整库恢复明确不同，不混用。第四步把状态变化和错误反馈接到实际使用入口，明确何时允许用户再次操作。第五步运行本任务的正常和反例用例，复核不应发生的副作用，而不是只观察表面结果。

最后执行与本任务相关的类型检查、单元或集成测试，并在必要时跑浏览器。局部实现通过后再执行上一个Gate的冒烟回归，防止共享模块改动破坏已经可用的采集与数据路径。

## 七、状态、错误与一致性检查

重点观察：schemaVersion、记录数量、ID映射、秘密排除与恢复前后数据库。在测试前记录基线，在测试后比较目标变化与未授权变化。发生错误时必须遵循统一错误协议，不能把异常吞掉后返回空成功，也不能用随机重试掩盖不确定的提交状态。

关键负向要求：验证不写入，失败不留半包数据，下载和恢复不触发模型调用。文件能打开不代表可恢复；必须检查关联完整、版本兼容与失败时原状态。若当前任务涉及外部依赖，不可用时给出明确降级或阻塞说明；无需外部依赖的部分仍应保持可用。

## 八、验收、证据与恢复

本任务有6个具名验收场景，详见独立用例文档。执行观察方式：将下载内容重新解析，检查字段集合和引用闭包，再在第二个临时库恢复并对比领域语义。验收证据包含实际输入或最小fixture、动作、预期与实际结果、命令退出码、相关实体ID或版本以及失败定位。

清理与恢复：删除测试下载与临时恢复库，保留脱敏校验报告；真实备份原件不能被测试覆盖。没有执行的用例只能标为未执行或环境阻塞。使用测试替身的结果不得写成真实外部服务已经通过；模拟的成功响应也不能出现在正式产品中冒充功能。

## 九、交接给下一个任务

交接时说明新增公开函数、DTO或组件的名字，列出已验证规则和仍阻塞的事项。不要把实现细节藏在临时聊天记录里。后续任务只应依赖本任务稳定输出，不应读取私有内部状态或复制SQL。改变契约必须同步其权威文件和所有受影响用例。

完成标准是：规则有实现位置，用例有执行证据，错误有可恢复路径，共享数据没有新增歧义。不能用“页面已经画出来”代替这个标准。


## 十、本任务的最小上下文包

[无秘密备份与恢复](../../03_contracts/10_backup_bundle.md)、[Key、本地请求和出站边界](../../03_contracts/05_settings_and_security.md)。这些是本任务需要读取的权威细节，不要求展开其他全部任务。更改公共协议先更新对应契约，再更新代码和用例。
