# G6 门禁报告（进行中）

状态：**G6 部分完成**。T070–T077 已实测通过并升为 `verified`；**T078–T084 尚未开始**。
本文件在 G6 全部完成前**不是**一份通过报告，不要按通过引用。

`implementation/progress/tasks.current.json` 把 T070–T077 改成 `verified` 并填了 `evidence`；
T042 按 G-2 词表从 `implemented` 改为 `blocked`（真实原因见 `evidence/G2.md`，
真实模型语义验收需要用户自己的 Key）；T078 及以后保持原状。

实测命令、退出码与逐用例结论在同名证据文件 [evidence/G6.md](../../implementation/progress/evidence/G6.md)。

> **本报告的当前范围**
>
> 本文件覆盖 **T070–T076**（整库逻辑导出、恢复校验、恢复事务、备份与损坏恢复手册、
> 本地诊断与可观测性、密钥与跨站安全回归、领域单元测试与边界矩阵）。
> **这四批的证据强度不同，不要混读**：
>
> - **T070–T073** 由主执行者亲手实现并逐条运行：此前派给子代理的同一批任务返回了
>   「success」但磁盘上没有任何文件改动（该项目已发生多次同类假成功），所以这一批
>   不引用任何他人摘要。
> - **T074** 由该任务的实施者完成（主执行者本轮不再独自包办，以推进剩余的 T075–T084）。
>   主执行者**之后独立复核**过：文件存在性、`typecheck`/`lint`/`contracts`/`build`、
>   `npm test`（72 套件 944 例 exit 0）、`npx playwright test`（110 passed / 1 skipped exit 0）、
>   变异残留、limits 双登记、以及那条「修掉真实缺陷」的断言能否守住。
>   复核记录与遗留缺口在 [evidence/G6.md](../../implementation/progress/evidence/G6.md)
>   的 T074-9 节，其中包含**主执行者自己写错的一处正则与三处过期注释**。
> - **T075** 由该任务的实施者写完代码与测试，但在**写文档之前被用户中止**；主执行者接手后
>   **自己重跑全部门禁、补做了原本缺失的两处变异对照、逐条复核断言归属**才写结论。
>   数字与缺口见 [evidence/G6.md](../../implementation/progress/evidence/G6.md) 的 T075-* 节。
>   T075 的 CSP **未启用**是具名缺口，不是已完成项。
> - **T076** 由主执行者实现。先做缺口分析再写测试：R04 已由上游覆盖故**不重复**，
>   实际补的是三处真空（`evidence.ts` 零公共覆盖、长度边界无法区分码点与 UTF-16 单元、
>   C04 守卫偶发）与一处**从约定变成强制**的装置（单元测试禁外网）。
>   它的四组变异对照与两个被自己用例抓出的缺陷在 evidence 的 T076-* 节。
> - **T077** 由主执行者实现，收尾时补齐了暂停记录里的两条具名缺口：C02（第二个连接 +
>   `db.prepare` 拦截造确定性交错）与 C03（注入点推到 `INSERT INTO relations`），
>   并给 C01 补了变异对照。补齐过程中查出一个**真实产品缺陷**：并发输家撞唯一约束时，
>   `withTransaction` 已把 sqlite 文本改写成 `数据唯一性冲突`，而 `createCapture` 只匹配原始文本，
>   于是分支永远进不去、用户看到 500 而不是重放。四组变异对照与细节在 evidence 的 T077-8 节。

## 1. 范围与依赖位置

G6 是「恢复、安全与交付」。按 `tasks.json`，前四项构成一条链，第五、六项是诊断与安全，
第七项是单元测试矩阵：

```
T070 整库逻辑导出与秘密白名单   （依赖 T010, T053, T069）
  └─ T071 恢复文件校验与空库策略 （依赖 T070）
       └─ T072 恢复事务、引用重建与回滚 （依赖 T071）
            └─ T073 WAL备份、数据搬迁与损坏恢复说明 （依赖 T070, T072）

T074 本地诊断与可观测性 （依赖 T041 的运行诊断投影）
T075 密钥、跨站与渲染安全回归 （依赖 T007, T028, T030, T057, T065, T070, T074）
T076 领域单元测试与边界矩阵 （依赖 T037, T056, T064, T075）
  └─ T077 API 与 SQLite 集成测试（依赖 T070, T071, T072, T076）
```

T077（集成套件）**已验收**；其余七项（T078–T084，六页 e2e、性能、Windows 手册、
生产审计、UX 终审、发布证据、最终验收）**尚未开始**，本文件不为其背书。

前置状态：T010（错误协议）、T053（视图快照）、T069（流程闭环验收）在各自 Gate 均已 verified，
因此本链的前置是可验证输出而不是「文件名存在」。

## 2. 逐任务状态

| 任务 | 内容 | 实现位置 | 证据位置 | 状态 |
| --- | --- | --- | --- | --- |
| T070 | 整库逻辑导出与秘密白名单 | `src/domain/exportBundle.ts`、`src/server/services/exportKnowledge.ts`、`src/app/api/export/route.ts` | `tests/integration/export.test.ts` 23 例（覆盖 `T070-C01…C06`） | **verified** |
| T071 | 恢复文件校验与空库策略 | `src/domain/importBundle.ts`、`src/server/services/validateImport.ts`、`src/app/api/import/validate/route.ts` | `tests/integration/import-validation.test.ts` 26 例（覆盖 `T071-C01…C06`） | **verified** |
| T072 | 恢复事务、引用重建与回滚 | `src/server/services/importKnowledge.ts`、`src/app/api/import/route.ts` | `tests/integration/import.test.ts` 26 例（覆盖 `T072-C01…C06`） | **verified** |
| T073 | WAL备份、数据搬迁与损坏恢复说明 | `scripts/inspect-data.mjs`、`docs/operations/backup-recovery.md` | `tests/integration/recovery.test.ts` 20 例（覆盖 `T073-C01…C06`） | **verified** |
| T074 | 本地诊断与可观测性 | `src/app/api/diagnostics/route.ts`、`src/server/observability/diagnostics.ts`、`src/features/settings/DiagnosticsPanel.tsx` | `tests/integration/diagnostics.test.ts` 29 例 + `tests/unit/diagnostics.test.ts` 19 例 + `tests/e2e/diagnostics.spec.ts` 6 例（覆盖 `T074-C01…C06`） | **verified** |
| T075 | 密钥、跨站与渲染安全回归 | `docs/security-checklist.md`、`src/server/llm/providerErrors.ts`（修缺陷）、`vitest.config.ts`（新增 security project） | `tests/security/` 8 文件 103 例（`T075-C01…C06`）+ `tests/e2e/security.spec.ts` 9 例；两处变异对照各红 5 条 | **verified**（CSP 未启用，具名缺口见 T075-6） |
| T076 | 领域单元测试与边界矩阵 | `tests/unit/evidence-contract.test.ts`、`tests/unit/text-boundaries.test.ts`、`tests/unit/networkIsolation.test.ts`、`tests/unit/support/networkGuard.ts`、`docs/test-coverage-map.md` | 新增 28 例（15+6+7）；四组变异对照；`docs/test-coverage-map.md` 是必交产物 | **verified** |
| T077 | API 与 SQLite 集成测试 | `tests/integration/user-data-guard.test.ts`（新）、`tests/integration/freshness-route.test.ts`（新）、`tests/integration/edit-item.test.ts`（+1 例）、`tests/integration/capture.test.ts`（+2 例）、`tests/integration/organize-service.test.ts`（+1 例）、`docs/api-test-map.md`（新，必交产物） | 新增 17 例（C06 7、C04 6、C01 1、C02 2、C03 1）；五处变异对照 C04 红 2 / C06-A 红 2 / C06-B 红 1 / C02-A 红 2 / C02-B 红 2 / C03 红 2 / C01 红 7，均还原复绿；C05 复用 `import.test.ts` `T072-C03` | **verified** |
| T078 | 六页浏览器端到端验收 | `tests/e2e/backup-restore.spec.ts`（新，4 例）、`tests/e2e/narrow-and-ime.spec.ts`（新，3 例）、`tests/e2e/support/consoleWatch.ts`（新 fixture + 3 条豁免）、`tests/e2e/support/fixtures.ts`（`consoleWatch` 转 `auto`）、`docs/browser-test-map.md`（新，必交产物） | 八场景映射 + 六用例逐条入 `evidence/G6.md` T078-2；e2e 121 → 132 passed；`chromium-narrow` 0 → 4 passed；探针实测 T078-C06 | **verified**（真实模型场景 R04 未执行，并入 T042 `blocked`；产物脱敏为具名残留缺口） |
| T079–T084 | 性能 / 手册 / 审计 / UX / 发布证据 / 最终验收 | — | — | **未开始** |

## 3. T070 已通过的硬性约束

- **T070-C01 完整字段**：人工锁定字段、原文、修订、**被拒绝的关系**都逐字段往返。
  夹具 `tests/helpers/backupLibrary.ts` 刻意种了这些边界（人工锁定、过期条目、
  被拒关系、来源已被删除的视图），而不是只测一条干净记录。
- **T070-C02 秘密白名单**：导出用**显式白名单列**（`BACKUP_*_COLUMNS`）而非 `SELECT *`，
  产物里没有 API Key、`settings` 行、Run 快照。这条由扫描用例**和**变异实验双重确认
  （证据文件 3.1：让导出多带一个字段就立刻红 2 条）。
- **T070-C03 一致快照**：整个读取在一个事务内完成，条目/标签/关系/视图之间的引用自洽，
  不会导出「改了一半」的库。
- **T070-C04 超限**：超过 `exportBytesMax`（20 MiB）时返回 **422 `VALIDATION`**，
  不是 500，也不产出半个包。上限值同时登记在 `reference/contracts/limits.json` 与
  `src/domain/limits.ts`，由 `npm run contracts` 比对。
- **T070-C05 中文保真**：中文与 emoji 按字节保真。
- **T070-C06 无配置依赖**：没有模型配置、没有任何 Run 也能完整导出。

## 4. T071 已通过的硬性约束

- **T071-C01 未知版本**：未来 `schemaVersion` **明确拒绝**并说明版本不支持，
  不猜测兼容——猜测兼容会把「结构可能已经变了」的数据静默灌进用户库。
- **T071-C02 重复 ID**：条目、标签、关系三类重复 UUID 全部被拒。
- **T071-C03 悬空关系**：关系的条目引用必须存在（**错误**）；视图缺来源只报**警告**
  （这是合法状态，视图可以指向已被删除的条目，T053 已确立）。
  两者区别对待由 `validateBundle` 分别判定，不是同一条规则。
- **T071-C04 非空目标**：目标库已有内容时拒绝恢复，**包括只有孤立标签**的情况
  （否则会留下一批无人引用的标签）。
- **T071-C05 大文件**：超限请求体在**流式读取阶段**就以 `BODY_TOO_LARGE` 拒绝，
  不先读进内存再判断。
- **T071-C06 只读验证**：校验不改库、不动 `datasetRevision`。这是「校验」与「恢复」
  分成两个端点的根本理由，也是它可被反复调用的前提。

## 5. T072 已通过的硬性约束

- **T072-C01 往返恢复**：导出→恢复后数据与引用完全一致，`datasetRevision` 递增。
- **T072-C02 中途失败**：事务中途报错时**全部回滚**，不留下半份数据。
  变异实验（证据文件 3.2）把 `ROLLBACK` 换成空操作后立刻红 1 条。
- **T072-C03 校验后变化**：校验通过后目标又变非空（或包哈希不符）时，
  在**事务内重新复查**并拒绝。这条防的是「校验与提交之间被别的会话插了数据」的窗口，
  因此复查必须在事务内而不是复用校验端点的结果。
- **T072-C04 秘密保持**：恢复**不导入 Run**，也**不覆盖**目标的 `settings` / `secrets`。
  目标的这两张表在恢复前后逐字节一致。
- **T072-C05 不自动整理**：恢复不触发任何模型调用。这条比「没有网络请求」更强——
  它断言不产生 Run 记录，因此连「打算调用但失败了」也排除在外。
- **T072-C06 引用 ID 保持**：视图快照、关系证据、标签位置都指向正确原文。

## 6. T073 已通过的硬性约束

- **T073-C01 活跃 WAL**：有未检查点的 WAL 时脚本如实报告，不误称库已完整落盘。
- **T073-C02 目录搬迁**：文档给出的搬迁步骤与实际可搬性一致，覆盖空格与 CJK 路径。
- **T073-C03 损坏文件**：损坏库被识别为 `damaged` 并给出**真实的 SQLite 报错**，
  而不是笼统的失败。
- **T073-C04 空库误读**：空目录、路径不存在都**不**被误报为损坏（退出码 0）。
  诊断工具把「没问题」误报成「坏了」会直接导致用户去删一个好库。
- **T073-C05 分享风险**：文档**明确警告**分享 `brain.db` 会泄露明文 Key——
  因为 Key 就在库里，而 `brain.db` 看起来只是个数据库文件。
- **T073-C06 恢复校验**：复制式恢复后可用 `inspect-data.mjs` 做语义校验。

**非破坏性是机械保证的**：`inspect-data.mjs` 以 `readOnly: true` 打开数据库，
不发任何会写盘的 PRAGMA（无 `wal_checkpoint`、无 `VACUUM`）。
变异实验（证据文件 3.4）把它改成可写并插一条 `PRAGMA user_version = 1` 后，
「检查前后字节完全一致」等 4 条立刻失败。

## 6b. T074 已通过的硬性约束

- **T074-C01 受保护读取**：`/api/diagnostics` 走与其它私有 API **同一道本地守卫**。
  无令牌与错令牌都返回 **403 `SESSION_EXPIRED`**，且拒绝体里没有 schemaVersion、
  能力清单、模型 host 或任何配置字段；守卫在 handler 之前执行，所以连一次数据库
  读取都不发生（用例对比了拒绝前后的日志计数）。
  「叫 health 就可以公开配置」在这里被明确拒绝：`/api/health` 保持公开但只说
  应用名、健康标志与协议版本。
- **T074-C02 数据库锁归存储层**：第二个连接用 `BEGIN IMMEDIATE` 占住写锁后，
  `database.lockProbe === 'locked'`，失败层级含 `storage` 且**不含** `model`，
  给出的首要动作是「释放写锁」而不是「检查模型名」或「重装依赖」。
  同一用例还断言持有写锁期间整库指纹（各表行数 + `dataset_revision`）逐项不变，
  证明这次读取没有改动知识、运行或设置。
- **T074-C03 渲染尺寸问题**：真实 200 响应到达浏览器、内容确实渲染出来之后，
  把被测量容器压成 0 高度，客户端诊断仍报出渲染尺寸问题，并明写
  「网络成功不代表可视化成功」。宽度为 0 同样成立（只报高度会漏掉一半条件）；
  尺寸正常时不报（反例，避免恒真）。
- **T074-C04 有界日志与限流**：保留上限（200 条，FIFO）与重复限流
  （1 分钟内同一键最多打印 3 次）都是契约值，响应里连同 `retention.policy`
  一起回给用户核对。15 次同类失败只打印 3 次、其余计入 `suppressedRepeats`。
- **T074-C05 分享边界**：响应与可粘贴摘要都逐字扫描，不含 API Key、私人原文、
  完整 endpoint 路径/query、会话令牌或绝对路径。数据目录只报
  `{kind, exists, writable}`——**没有可以承载路径的字段**，不是过滤掉。观测不到的
  计数保留 `null` 并渲染为「未知」，不填 0。
- **T074-C06 无遥测**：使用全部六个页面期间，`traffic` 记录的外部请求与被拦下的
  外网尝试都是空数组，所有请求 host 均为 `127.0.0.1`；报告自身声明 `telemetry: 'none'`，
  且响应里没有 sentry/posthog/analytics 等平台痕迹。

**只读是机械保证的**：唯一写动作是 `probeWriteLock`（`BEGIN IMMEDIATE` + `ROLLBACK`，
并恢复原 `busy_timeout`）。变异实验（证据文件 T074-4）把重复限流解除后
C04 立刻红（`expected 15 to be 3`），把 endpoint 从 host 放宽成完整 URL 后
C05 立刻红（摘要里出现 `tenant-secret-path`）。

## 7. 本轮发现的真实缺陷（摘要）

完整现象/根因/后果/修复/回归见证据文件第 5 节。三条都不是「实现没写」，
而是「已有的东西在特定条件下会骗人」：

1. **契约守卫用例会随进度静默失效**（`tests/contracts/guard.test.ts` 的 T012-C06）。
   该用例把 `/api/export` 写死成「尚未实现」，T070 落地后前提为假、用例直接红。
   更早它写死的是 `/api/graph`，G3 实现后那句替换不再命中、用例**已经静默失效过一次**
   （用例自己的注释就记着这件事）。也就是说「实现一个契约端点」当时会同时产生
   「一条红」或「一条空转」两种结果，取决于替换是否命中。
   已改为从 `PENDING_ROUTES` **动态**挑一条仍未实现的端点，随进度自然迁移。
   T074 把待办表清空后这个动态挑选本身会落到本地扩展路由的 `{id}` 上并因大括号未转义
   而抛 `Incomplete quantifier`，因此这一轮又补了三处：匹配范围限定在 `PENDING_ROUTES`
   块内、加 `escapeRegExp`、待办表为空时临时注入一条假路由来验证守卫规则本身。
   （同一个坑第二次绊到人，说明「从真实清单里挑样例」需要显式的空集分支。）
2. **T073 损坏库断言的机制写错了**。我最初断言 `readable: true` 且 `integrity: 'damaged'`，
   实测是 SQLite 接受打开但一查询就抛错，故 `readable: false`。已按真实机制改正，
   且**没有**退化成「damaged 或 unreadable 都接受」的松断言——那会让「脚本压根没打开文件」
   也蒙混过关。
3. **lint 残留 2 条 warning**（`flow-history.test.ts` 的无用参数与导入）。已清零，
   全仓库 `npm run lint` 现在 0 problems。
4. **诊断面板把唯一的排查入口放进了被它自己报告的盒子里**（T074）。被测容器就是整个
   面板外层 wrapper，而「重新检查渲染尺寸」按钮也在里面；容器被压成 0 高度后按钮自己也
   不可点击——面板唯一能解释「页面为什么空白」的交互入口，被它要报告的那个条件挡住了。
   unit 侧断言全绿，只有真实浏览器能发现。已把测量面与控件拆成两个盒子，
   断言一个字没改。

## 8. 本轮修复的测试自身缺陷（不是产品缺陷）

- **T070 超限用例写错了两处**：一是试图用**单个**条目撑过 20 MiB，撞上
  `length(captured_text) BETWEEN 1 AND 10000` 的 schema 上限；二是批量插入的
  `capture_request_id` 与夹具已有记录撞了 UNIQUE 约束。改为「多条各自合法的条目
  把**库**撑超预算」，并用独立 ID 前缀。这才是用例原文「整库超限」的意思。
- **T070 哈希用例原本是空转的**：它用 `JSON.stringify` 的 replacer 数组想打乱键序，
  但那其实是**过滤**键而不是重排，于是「键序不敏感」这条恒真。
  已换成递归反转键序的函数，并**额外断言**反转后的对象键序确实不同。
- **T071 的 harness 只有一个库**：它先用夹具把库填满，再拿这个库当「恢复目标」，
  而契约要求恢复到**空**库，于是 5 条用例在断言「非空被拒」时失败。
  已改成 `source`（导出用）与 `target`（干净目标）两个库，这才是真实恢复场景的形状。
- **T072 的证据引文不是原文子串**：夹具里的引文 `清楚地定义问题和验收结果`
  并不在种下的原文里，断言必然失败。已改为原文中的真实片段 `越需要把问题说清楚`。
  这类错误的危险在于「用夹具迁就实现」——所以改的是**引文**（对齐真实原文），
  而不是删断言。

## 9. 全量回归

```
npm run typecheck   exit 0
npm run lint        exit 0（0 problems，全仓库）
npm run contracts   exit 0（failures: []，routes.pending 从 4 降到 1，T074 后降到 0）
npm run build       exit 0
npm test            exit 0   70 套件 / 896 例        ← T070–T073 时
npm test            exit 0   72 套件 / 944 例        ← T074 后（+48 例）
npx playwright test exit 0   104 例（3.6m）          ← T070–T073 时
npx playwright test exit 0   110 例 / 1 skipped      ← T074 后（+6 例）
```

T074 新增的 48 例 vitest 来自 `tests/integration/diagnostics.test.ts`（29）与
`tests/unit/diagnostics.test.ts`（19）；新增的 6 例 Playwright 来自
`tests/e2e/diagnostics.spec.ts`。Playwright 那 1 个 skip 是 `gate2.spec.ts` 里
原有的条件跳过（要求「未配置模型」前置），不是本轮引入。

`npm test` 的 **exit 0 是本轮新增确认**：G5 时它虽然 548/548 全过却 exit 1，
根因是 `afterAll` 里的同步 `rmSync` 阻塞了 vitest 事件循环（`docs/progress/G5.md` 第 7.2 节）。
修掉后本 Gate 的 896 例是干净 exit 0，G5 报告里那条「需另行处理」的遗留因此关闭。

## 10. 未执行 / 阻塞

- **T077 已验收**（六条用例都有实测入口，C01/C02/C03/C04/C06 有变异对照），
  本轮查出的并发缺陷与四组变异重量见 evidence T077-8。
  `tests/helpers/database.ts` 仍**未按 `targetFiles` 字面创建**：既有 `tests/helpers/db.ts`
  承担同一职责，不为文件名再造一份（记录在 `docs/api-test-map.md` 第 4 节）。
- **T078 已验收**，但有三处**具名未覆盖**，不得读成已覆盖：
  1. **R04 真实 Provider 场景未执行**：套件里没有 `BRAIN_E2E_REAL_MODEL` 开关，本轮也没有
     新增一段"永远跳过"的代码充当已实现。真实语义验收并入 T042，仍 `blocked`。
  2. **产物脱敏未实现**：探针实测 Playwright trace 会归档**已提交的请求体**，因此用例里填过
     真 Key 时 `test-results/` 会落盘明文。产品侧三处（密码框、GET 响应、导出字节）实测均无明文，
     但装置侧这一步没做——`docs/browser-test-map.md` 第 4.5 节列为残留缺口。
     `test-results/` 已被 `.gitignore` 忽略、从不提交、不进证据目录。
  3. **真实输入法人工检查未执行**：候选窗、拼音上屏、全角标点无法在无头 Chromium 里复现，
     待执行清单在 `docs/browser-test-map.md` 第三节，如实标为「未执行」。
- **T079–T084**：未开始。依次串行（T079 依赖 T050/T057/T066/T078，T081 依赖 T075/T078/T079/T080）。
- **T075 的 CSP 未启用**：实测结论，且有具体原因——Mermaid 依赖自己注入的内联 `<style>`
  来配色，而净化合同要求留住 `#a{fill:#fff}` 这类惰性样式表，所以不留 `style-src` 空间的
  严格 CSP 会直接破坏图形视图。要启用得先设计 Mermaid/Markmap 的样式策略（nonce 化注入，
  或把配色移到允许属性上），那是独立工作。`docs/security-checklist.md` 如实写着「未启用」，
  并由 `tests/security/threat-model-honesty.test.ts` 断言文档与代码都没有声称已启用。
- **T075 不覆盖的三类威胁**（文档第四节逐条写明，并由测试断言其存在）：操作系统已被入侵、
  恶意同源脚本、用户主动把 Key 交给不可信服务。另两条边界：Key 是本机**明文**存储、
  **无硬件级保证**；令牌与来源检查只在 HTTP 层，同机可读用户目录的进程不受约束。
- **T074 的日志文件轮转未实现**：诊断日志 sink 是**进程标准输出**，不写日志文件
  （设计选择，见证据文件 T074-3）。若将来要求落盘，需要新契约而不是在实现里偷偷加；
  「日志有界」目前由内存保留上限 + 重复限流两条界保证，并经 C04 实测。
- **T074 未覆盖 JS 运行时异常上报**：C03 覆盖的是「容器塌陷」这类渲染尺寸问题，
  renderer 只按 `kind` + 版本列出（读权威常量）。新增前端错误上报管道属于
  T065/T068 的渲染安全范围，本任务没有越界实现。
- **真实模型语义质量**：`BLOCKED_BY_EXTERNAL_CREDENTIAL`，与 G2/G5 相同。
  本 Gate 的六个任务**不需要**真实模型——T070-C06、T072-C05、T074-R03/C06 与 T075-C04
  恰恰断言的是「没有模型配置也能导出」「恢复不触发模型」「不发起任何外部请求」
  「重定向被拒且 Key 不转发」；T075-C04 用的是**本机回环 302 服务**，不是真实 Provider。
  所以在无 Key 环境下结论是完整的。
- **损坏盘的人工作业未演练**：自动化覆盖了脚本行为与文档关键内容断言，
  但无法在 CI 里模拟一块真的坏盘。记为「文档已校验，人工作业未演练」。
- **G6 未完成 ⇒ 本文件不是通过报告**。T083（任务证据与缺陷清单）与
  T084（最终用户旅程与 MVP 完成定义）是 G6 的收尾，尚未开始。

## 10b. 全量门禁（T075 交付后实测）

```
npm run typecheck                     exit 0
npm run lint                          exit 0（0 problems，全仓库）
npm run contracts                     exit 0   implemented 23 / pending 0 / failures []
npm run build                         exit 0
npm test                              exit 0   78 文件 / 1023 例
npx playwright test                   exit 0   119 passed / 1 skipped
```

用例数变化轨迹可见「测试只增不减」：G5 时 548 例 → G6 前四项后 896 例 →
T074 后 944 例 → T075 后 **1023 例**。搬迁 `tests/integration/*.test.ts` 到
`tests/security/` 时数目是**增加**的，若减少就说明有测试被搬丢。

## 10c. 全量门禁（T077 收尾时实测，2026-09-16）

```
npm run typecheck                     exit 0
npm run lint                          exit 0（0 problems，全仓库）
npm run contracts                     exit 0   pending 0 / failures []
npm run build                         exit 0
npm test                              exit 0   83 文件 / 1071 例
  分 project 单跑                              unit 392 / integration 534 / security 103 /
                                               contracts 32 / browser 10
                                               392+534+103+32+10 = 1071 = 整跑 ✓
npx playwright test gate5 + gate4     exit 0   13 passed（上一 Gate 冒烟）
```

轨迹：T076 后 1054 → T077 收口 1068 → T077 收尾 **1071**（+3：C02 两例、C03 一例；
C01 只补断言行不增例）。五个 project 之和仍等于整跑，
一旦不等就说明某个 glob 收集不到文件（静默不跑，不报错）。

## 10d. 全量门禁（T078 交付后实测，2026-09-16）

```
npm run typecheck                     exit 0
npm run lint                          exit 0（0 problems，全仓库）
npm run contracts                     exit 0   pending 0 / failures []
npm test                              exit 0   83 文件 / 1071 例
  分 project 单跑                              unit 392 / integration 534 / security 103 /
                                               contracts 32 / browser 10
                                               392+534+103+32+10 = 1071 = 整跑 ✓
npm run build                         exit 0
npx playwright test                   exit 0   132 passed / 1 skipped（全量，含 chromium-narrow）
npx playwright test --project=chromium-narrow
                                      exit 0   4 passed
```

T078 **不新增 vitest 例**（它是浏览器端验收，断言落在 Playwright 侧），所以 1071 不变；
e2e 从 121 增至 **132**（+11：`backup-restore` 4、`narrow-and-ime` 3、
`chromium-narrow` project 新收 4；`chromium-narrow` 此前是 `exit 1 No tests found`）。

1 条 skip 仍是 `gate2.spec.ts` 的既有条件跳过（要求"未配置模型"这一前置），
不是本轮引入，也不属于 T042 的真实语义组。

## 11. 交接给下一个任务

1. **`BACKUP_SCHEMA_VERSION = 1`**（`src/domain/exportBundle.ts:51`）。恢复侧对未来版本
   **明确拒绝**；改这个常量必须同步改 `importBundle.ts` 的版本分支与 T071-C01 用例。
2. **导出上限双登记**：`exportBytesMax` 同时在 `reference/contracts/limits.json` 与
   `src/domain/limits.ts`，契约检查比对两者，只改一处会让 `npm run contracts` 失败。
3. **恢复请求体上限走 `bodyLimit: 'import'`**（`src/server/http/respond.ts` 的
   `ROUTE_BODY_LIMITS` 按路由族区分），不是全局默认值；新增同类大体积端点应复用该 kind。
4. **`inspect-data.mjs` 故意是 `.mjs` 而非 `.ts`**：它必须能在没有构建、没有 TypeScript
   的环境直接运行，否则「数据库坏了且服务起不来」时就没用了。因此它不参与 `tsc`，
   改动后用 `tests/integration/recovery.test.ts` 兜底。
5. **T070 的导出白名单是新增字段的关口**：任何新的知识字段若要进入备份，
   必须同时进入 `BACKUP_*_COLUMNS` 与 `exportBundle.ts` 的类型，否则会静默丢失；
   反之若字段本身是秘密，加进白名单会让 T070-C02 立刻红——这个张力是有意的。
6. **T074 的 `DiagnosticsReport` 同样是白名单，而且有两层**：服务端 DTO
   （`src/domain/api.ts`）与前端可粘贴摘要（`DiagnosticsPanel.tsx` 的
   `toDiagnosticsExport`，逐字段命名）。不要把它改成 `JSON.stringify(report)`——
   那会一次性打开 Key、原文与完整 URL 三条口子。想加字段必须同时决定它能否进摘要。
7. **`/api/diagnostics` 只读，唯一写动作是 `probeWriteLock`**（`BEGIN IMMEDIATE` +
   `ROLLBACK`，并恢复原 `busy_timeout`）。新增诊断字段时不要顺手加写操作，
   T074-C02 的整库指纹对比会红。
8. **测量面与它的控件必须分属两个盒子**：`data-testid="diagnostics-surface"` 是被测量
   的内容区，外层 wrapper 承载「重新检查渲染尺寸」按钮且不参与测量。把按钮挪进测量区，
   容器塌陷时它自己也点不到，而 C03 的单测仍会绿——这是本轮真实修过的缺陷。
9. **安全断言现在只有一个家：`tests/security/`**（独立的 `security` vitest project）。
   T007 的 `security-http.test.ts` 与 T030 的 `secret-redaction-wiring.test.ts` 已**移动**
   到这里（git 记为 RM），不是复制。新增安全回归请放进这个目录；若新建目录/改名，
   必须同时确认 `vitest.config.ts` 的 project glob 仍然收集它——**移到没人收集的目录会静默不跑**，
   这是本节最需要防范的失败模式（所以全量例数是增是减要一起看）。
10. **`redirect: 'error'` 是 C04 的实际防线**（`src/server/llm/transport.ts:97`）。
   变异对照把它改回 `'follow'` 会让 5 条用例变红，所以这条配置不是装饰。
   同时 `providerErrors.ts` 的 `errorChainText` 会遍历 `cause` 链——Node 的 fetch 把
   拒绝重定向报成 `TypeError('fetch failed')`，**只看 `error.message` 会把重定向误诊成网络问题**
   并错误宣称「可能已计费」。新增传输错误分支时请继续读 cause 链。
11. **CSP 未启用是有依据的现状，不是疏漏**。不要在后续任务里顺手打开一个严格 CSP：
   Mermaid 需要内联 `<style>` 配色，盲开会直接破坏图形视图。
   若真要启用，先设计样式策略（nonce 化注入或把配色移到允许属性），并同步改
   `docs/security-checklist.md` 与 `tests/security/threat-model-honesty.test.ts`——
   后者会断言文档写着「未启用」，两边必须一起改。